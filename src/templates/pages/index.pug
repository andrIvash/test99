extends ../template

block variables
  //- var title = 'Добро пожаловать'

block content
  p #{title}
  h1 Главная
  form#form
    label(for="email") 
    input(name="email" id="email")
    label(for="password") 
    input(name="password" id="password")
    button Войти
    button.clear(type="reset") Очистить